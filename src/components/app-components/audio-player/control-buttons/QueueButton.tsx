import QueueIcon from '@/assets/icons/QueueIcon';
import IconButton from '../../icon-button';

function QueueButton() {
    return <IconButton icon={QueueIcon} tooltipContent="Queue" />;
}

export default QueueButton;
