import IconButton from '../../icon-button';
import MiniPlayerIcon from '@/assets/icons/MiniPlayerIcon';

function MiniPlayerButton() {
    return (
        <IconButton icon={MiniPlayerIcon} tooltipContent="Open Mini PLayer" />
    );
}

export default MiniPlayerButton;
